---
# TODO Will fail due to unused ELB and corresponding SG
- name: "Delete all unused groups"
  ec2_group:
    group_id: "{{ item }}"
    state: absent
  with_items: "{{ unused_group_ids }}"
  when: unused_group_ids|length > 0

...
